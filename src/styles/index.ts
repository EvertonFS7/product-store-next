import { createStitches } from '@stitches/react'

export const {
    config,
    styled,
    globalCss,
    createTheme,
    css,
    keyframes,
    getCssText,
    theme
} = createStitches({
    theme: {
        colors: {
            default: '#8257e6'
        }
    }
})